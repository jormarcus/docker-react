language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t jormarcus/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jormarcus/docker-react npm run test
